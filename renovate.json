{
  "extends": ["github>whitesource/merge-confidence:beta",":disableRateLimiting"],
  
  "major": {
    "enabled": false
  },
  "packageRules": [
    {
      "groupName": "all deps",
      "matchPaths": ["+(package.json)"]
    }
  ]
}
